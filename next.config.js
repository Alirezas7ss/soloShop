/** @type {import('next').NextConfig} */
const nextConfig = {
    i18n: {
        localeDetection: false, 
        locales: ["ir", "fa"], 
        defaultLocale: "ir"     
      }
}

module.exports = nextConfig
